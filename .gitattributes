# Keep Bash scripts in LF format: CRLF can cause "\r: command not found"
# errors on Linux and macOS. Explicit rules prevent Git's automatic
# line-ending conversion on Windows.
*.sh   text eol=lf
*.md   text eol=lf
*.yaml text eol=lf
*.ps1  text eol=crlf
*.zip  binary
